[build]
  base = "motionify-app"
  command = "npm install && npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Next.js App Router handles /v2 route automatically


